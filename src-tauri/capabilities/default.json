{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Minimal renderer permissions for the main Fable window",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
